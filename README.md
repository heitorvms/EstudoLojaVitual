# Loja virtual — gerenciamento de orçamento

Monorepo com duas pastas principais:

- **Backend** — API Spring Boot (Java).
- **Frontend** — interface React (Create React App).

Na pasta `Frontend`, copie variáveis de ambiente para um ficheiro `.env` (não versionado); veja a documentação do CRA para o prefixo `REACT_APP_`.
