import styled from "styled-components";
import { Button } from "primereact/button";
import { ContainerPage as Cp, TitlePage as Tp } from "../GerenciamentoPerfilUsuario/styled";

export const ContainerPage = Cp;
export const TitlePage = Tp;

export const ButtonStyled = styled(Button)`
  max-width: 200px;
`;
